package minigames.navgraph 
{
	
	public class QuadTree extends QuadNode
	{
		
		public function QuadTree(depth:int, maxDepth:int, fromX:Number, fromY:Number, toX:Number, toY:Number) 
		{
			super(depth, maxDepth, fromX, fromY, toX, toY);
		}
		
	}

}